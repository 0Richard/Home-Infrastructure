# =============================================================================
# Pi-hole - Additional dnsmasq Configuration
# =============================================================================
# Location: /srv/docker/pihole/etc-dnsmasq.d/99-custom.conf
# =============================================================================

# Listen on all interfaces (LAN + WireGuard)
# Note: FTLCONF_dns_listeningMode=all in docker-compose handles interface binding
# Do NOT use "interface=" here as it restricts to ONLY that interface
